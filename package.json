{
  "name": "ts-axios",
  "version": "0.1.0",
  "private": true,
  "description": "Learn axios core step by step through git history.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "rimraf dist && tsc -p tsconfig.build.json"
  },
  "devDependencies": {
    "@types/node": "20.12.7",
    "rimraf": "5.0.5",
    "typescript": "5.4.5",
    "vitest": "1.5.0"
  }
}
